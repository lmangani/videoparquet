name: Test videoparquet

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        python-version: [3.11]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Install videoparquet (editable)
        run: |
          pip install -e .
      - name: Install pytest
        run: |
          pip install pytest
      - name: Install ffmpeg (brew)
        run: |
          brew install ffmpeg
      - name: Run tests
        run: |
          pytest tests/

  test-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Install videoparquet (editable)
        run: |
          pip install -e .
      - name: Install pytest
        run: |
          pip install pytest
      - name: Download and install static ffmpeg and ffprobe (evermeet.cx)
        run: |
          sudo apt-get update && sudo apt-get install -y p7zip-full
          wget --trust-server-names https://evermeet.cx/ffmpeg/get/ffmpeg/7z
          wget --trust-server-names https://evermeet.cx/ffmpeg/get/ffprobe/7z
          7z x ffmpeg.7z -offmpeg-evermeet
          7z x ffprobe.7z -offmpeg-evermeet
          sudo cp ffmpeg-evermeet/ffmpeg /usr/local/bin/
          sudo cp ffmpeg-evermeet/ffprobe /usr/local/bin/
          ffmpeg -version
          ffprobe -version
          ffmpeg -h encoder=ffv1 | grep gbrp
      - name: Run tests
        run: |
          pytest tests/ 
